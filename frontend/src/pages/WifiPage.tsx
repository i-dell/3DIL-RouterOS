import {Card,Page,State,useApi} from './shared.js';type W={supported:boolean;reason:string|null;bands:Array<{band:string;ssid:string|null;enabled:boolean|null;channel:number|null;security:string|null;guest:boolean}>};export const WifiPage=()=>{const q=useApi<W>('/api/v1/router/wifi');return <Page title="Wi‑Fi (للقراءة فقط)"><State loading={q.loading} error={q.error}/>{q.data&&!q.data.supported&&<p>{q.data.reason}</p>}<div className="grid gap-4 md:grid-cols-2">{q.data?.bands.map((b,i)=><Card key={i} title={b.band} value={<dl><dt>SSID</dt><dd>{b.ssid}</dd><dt>الحالة</dt><dd>{b.enabled==null?'غير متاح':b.enabled?'مفعّل':'متوقف'}</dd><dt>القناة</dt><dd>{b.channel??'غير متاح'}</dd><dt>الحماية</dt><dd>{b.security??'غير متاح'}</dd></dl>}/>)}</div></Page>};
