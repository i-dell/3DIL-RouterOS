import {Page,State,useApi} from './shared.js';type L=Array<{timestamp:string;level:string;message:string}>;export const LogsPage=()=>{const q=useApi<L>('/api/v1/router/logs');return <Page title="السجل الآمن"><State loading={q.loading} error={q.error}/>{q.data?.map((x,i)=><div key={i} className="rounded-xl border border-emerald-900 p-3"><time>{x.timestamp}</time> · {x.level} · {x.message}</div>)}</Page>};
