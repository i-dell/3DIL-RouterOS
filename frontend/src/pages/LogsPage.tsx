import {Empty,Page,State,useApi} from './shared.js';type L=Array<{timestamp:string;level:string;message:string}>;export const LogsPage=()=>{const q=useApi<L>('/api/v1/router/logs');return <Page title="السجل الآمن" description="Sanitized local application events only"><button onClick={()=>void q.reload()} className="rounded-xl bg-emerald-700 px-4 py-2">Refresh</button><State loading={q.loading} error={q.error}/>{q.data?.length===0&&<Empty>No safe application events are available.</Empty>}<div className="space-y-2">{q.data?.map((x,i)=><div key={`${x.timestamp}:${i}`} className="grid gap-2 rounded-xl border border-emerald-900 bg-[#07120d] p-3 sm:grid-cols-[auto_auto_1fr]"><time dateTime={x.timestamp}>{new Date(x.timestamp).toLocaleString()}</time><span className="uppercase text-emerald-500">{x.level}</span><span>{x.message}</span></div>)}</div></Page>};
