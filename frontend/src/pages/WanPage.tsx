import {Card,Page,State,useApi} from './shared.js';type W={supported:boolean;reason:string|null;state:string|null;ip:string|null;gateway:string|null;dns:string[]|null;connectionType:string|null;uptime:number|null};export const WanPage=()=>{const q=useApi<W>('/api/v1/router/wan');return <Page title="WAN (للقراءة فقط)"><State loading={q.loading} error={q.error}/>{q.data&&<div className="grid gap-4 md:grid-cols-2"><Card title="الحالة" value={q.data.state} reason={q.data.reason}/><Card title="عنوان WAN" value={q.data.ip}/><Card title="البوابة" value={q.data.gateway}/><Card title="DNS" value={q.data.dns?.join(', ')}/><Card title="نوع الاتصال" value={q.data.connectionType}/><Card title="مدة الاتصال" value={q.data.uptime}/></div>}</Page>};
