import type {RouterDevice} from '../pages/routerTypes.js';import type {IconName} from '../config/icons.js';
export type DeviceConfidence='confirmed'|'probable'|'generic';export interface DeviceIdentity{category:string;manufacturer:string|null;icon:IconName;confidence:DeviceConfidence;evidence:string[]}
const includes=(text:string,terms:string[])=>terms.some(term=>text.includes(term));
export const identifyDevice=(device:RouterDevice,local?:{category?:string;manufacturer?:string;icon?:IconName}):DeviceIdentity=>{if(local?.category)return{category:local.category,manufacturer:local.manufacturer??null,icon:local.icon??'unknown',confidence:'confirmed',evidence:['local SQLite metadata']};const text=[device.hostname,device.alias].filter(Boolean).join(' ').toLowerCase(),wired=!/wifi|ssid/i.test(device.connectionType??'');if(includes(text,['iphone']))return{category:'iPhone',manufacturer:'Apple',icon:'phone',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['ipad']))return{category:'iPad',manufacturer:'Apple',icon:'tablet',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['android','galaxy','phone']))return{category:'Smartphone',manufacturer:null,icon:'phone',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['macbook','imac']))return{category:'Mac',manufacturer:'Apple',icon:'computer',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['windows','laptop','desktop','pc-']))return{category:'Computer',manufacturer:null,icon:'computer',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['tv','bravia','chromecast','appletv']))return{category:'Smart TV',manufacturer:null,icon:'tv',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['playstation','xbox','nintendo']))return{category:'Game console',manufacturer:null,icon:'console',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['printer']))return{category:'Printer',manufacturer:null,icon:'printer',confidence:'probable',evidence:['router hostname or alias']};if(includes(text,['camera','cam']))return{category:'Camera',manufacturer:null,icon:'camera',confidence:'probable',evidence:['router hostname or alias']};return{category:wired?'Network device':'Wireless device',manufacturer:null,icon:wired?'computer':'iot',confidence:'generic',evidence:[device.connectionType?'router connection type':'insufficient router evidence']}};
