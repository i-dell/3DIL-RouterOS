import {NavLink} from 'react-router-dom';import {iconPaths} from '../../config/icons.js';import type {IconName} from '../../config/icons.js';
const items:Array<{to:string;label:string;icon:IconName}>=[{to:'/dashboard',label:'الرئيسية',icon:'dashboard'},{to:'/devices',label:'الأجهزة',icon:'devices'},{to:'/wifi',label:'Wi-Fi',icon:'wifi'},{to:'/security',label:'الأمان',icon:'security'},{to:'/settings',label:'المزيد',icon:'settings'}];
export const MobileBottomNav=()=> <nav aria-label="التنقل المحمول" className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-5 border-t border-[var(--border)] bg-[color:var(--surface-elevated)]/95 pb-[max(.5rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur-xl lg:hidden">{items.map(item=><NavLink key={item.to} to={item.to} className={({isActive})=>`flex min-h-12 flex-col items-center justify-center gap-1 text-[10px] transition ${isActive?'text-[var(--success)]':'text-[var(--text-secondary)]'}`}><svg aria-hidden="true" viewBox="0 0 24 24" className="h-5 w-5 fill-none stroke-current" strokeWidth="1.8"><path d={iconPaths[item.icon]}/></svg><span>{item.label}</span></NavLink>)}</nav>;
