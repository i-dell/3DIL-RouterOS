import assert from 'node:assert/strict';import test from 'node:test';import fs from 'node:fs';test('navigation routes use distinct pages',()=>{const source=fs.readFileSync(new URL('./App.tsx',import.meta.url),'utf8');for(const page of ['DevicesPage','WifiPage','WanPage','SecurityPage','SettingsPage','LogsPage'])assert.ok(source.includes(`element={<${page}/>`))});
