# Session Management

The schema stores only safe session identifier hashes, user reference, activity, expiry, and revoked state. Raw tokens are never returned. Session mutation remains disabled while local administration authentication is unconfigured. Huawei router cookies and session identifiers are never exposed through System APIs.
