# System Testing

Tests cover capability separation, AES-256-GCM/scrypt backup encryption and authentication, salted password hashes, permission evaluation, final-admin protection, exact confirmation, diff semantics, settings validation, snapshots, SQLite migrations, and integrity. Automated tests never call router reboot, restore, firmware update, or factory reset.
