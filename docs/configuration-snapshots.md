# Configuration Snapshots

Snapshots normalize real device, WAN, Wi-Fi, and security values with source, supported, verified, and collection timestamp metadata. Secrets are omitted or redacted. Diff results are changed, unchanged, or unavailable; missing values are never mislabeled as removed.
