# Alert Rules

Rules are evaluated only for enabled rules with valid fresh numeric data. Deduplication keys represent one unresolved condition; cooldown updates detection time without increasing occurrence count. No default rules or synthetic alerts are created. Persistent tables reserve rule, alert, acknowledgement, occurrence, and resolution state.
