# Diagnostics Testing

Tests cover capability mapping, hostname/IP/port validation, command and argument injection, unsafe URL schemes, loopback/link-local/metadata SSRF targets, output-derived ping values, and SQLite persistence. Release validation also includes all regression tests, production build, live Huawei authentication, runtime API checks, and browser UI inspection.
