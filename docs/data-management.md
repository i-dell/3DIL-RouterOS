# Data Management

System APIs report sanitized database and backup usage, expose immutable audit entries, and provide explicit integrity checks. Destructive retention, vacuum, backup deletion, and location deletion require confirmation or remain gated. Active database files cannot be directly deleted.
