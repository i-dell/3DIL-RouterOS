# System Management

System Management separates verified Huawei reads from local application administration. Router reboot, backup/restore, configuration writes, firmware update, and factory reset remain disabled because no Huawei mutation endpoint is verified. Local services provide encrypted backups, normalized configuration snapshots, safe settings, immutable audit reads, database integrity, usage visibility, and privacy controls.
