# Database Maintenance

`PRAGMA integrity_check` is available as a safe explicit action. Vacuum and retention reduction remain confirmation-gated to avoid blocking or silent deletion. The overview reports a sanitized relative path, size, schema version, backup readiness, and pending migrations.
