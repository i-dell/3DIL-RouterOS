# Network Health Score

The transparent score includes only router reachability, authenticated-session state, WAN connection state, and collection freshness when available. The result reports partial/full status, confidence, assessed and unavailable counts, evidence, and timestamp. CPU and memory are excluded.
