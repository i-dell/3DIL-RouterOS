# System API

`/api/v1/system` exposes capabilities, overview, router/application information, encrypted backups and validation, normalized snapshots and comparison, time/NTP visibility, allowlisted settings, logs, audit, data usage, integrity, privacy, and update readiness. Unverified router writes return `FEATURE_NOT_VERIFIED`; high-risk local actions require confirmation or re-authentication.
