import type {RouterSnapshot} from '../types.js';import {moduleResult} from './types.js';
export const readNat=(s:RouterSnapshot|null)=>moduleResult('nat',s,{enabled:null,rules:null,translationCount:null,sessions:null,statistics:null},false,null);
