import type {RouterSnapshot} from '../types.js';import {moduleResult} from './types.js';
export const readWifi=(s:RouterSnapshot|null)=>moduleResult('wifi',s,{radios:(s?.wifi.bands??[]).map(r=>({band:r.band,ssid:r.ssid,status:r.enabled,channel:r.channel,bandwidth:null,encryption:r.security,hiddenSsid:null,country:null,transmitPower:null,connectedClients:s?.devices.items.filter(d=>d.wifiBand===r.band&&d.online!==false).length??null,signalQuality:null,noise:null,utilization:null}))},s?.wifi.supported===true,s?.wifi.sourceEndpoint??null);
export const readGuestWifi=(s:RouterSnapshot|null)=>{const radios=(s?.wifi.bands??[]).filter(r=>r.guest);return moduleResult('guest-wifi',s,{radios:radios.map(r=>({enabled:r.enabled,ssid:r.ssid,security:r.security,bandwidthLimit:null,isolation:null,connectedGuests:null,schedule:null,qrCode:null,password:null}))},radios.length>0,s?.wifi.sourceEndpoint??null)};
